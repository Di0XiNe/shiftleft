name: Build, Scan, Push, and SBOM Generation

on:
  push:
    branches:
      - main
    paths:
      - 'Dockerfile' # El workflow se ejecuta solo si se modifica el Dockerfile

env:
  # 1. Definimos solo la variable estática aquí
  IMAGE_REPO: quay.io/harik_sc/shift_left 

jobs:
  build-scan-push:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      # 🟢 PAS DE CORRECCIÓ: Define las variables dinámicas y las exporta a GITHUB_ENV
      - name: ⚙️ Set Dynamic Image Tags
        run: |
          # Exportamos tags dinámicos utilizando la sintaxis de shell válida para GITHUB_ENV
          echo "IMAGE_TAG_SHA=${{ env.IMAGE_REPO }}:${{ github.sha }}" >> $GITHUB_ENV
          echo "LATEST_IMAGE_TAG=${{ env.IMAGE_REPO }}:latest" >> $GITHUB_ENV
          echo "Tags dinámicos establecidos."

      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔧 Construir la imagen del contenedor
        # 3. Utilizamos las variables exportadas
        run: |
          docker build -t ${{ env.IMAGE_TAG_SHA }} -t ${{ env.LATEST_IMAGE_TAG }} .

      # 1. 🔍 Escaneo Rápido (Trivy)
      - name: 🔍 Scan image with Trivy (Falla si hay HIGH/CRITICAL)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_TAG_SHA }}
          severity: HIGH,CRITICAL
          exit-code: 1
          ignore-unfixed: true

      # 2. 🧾 Generar SBOM y Escanear (Syft + Grype)
      - name: 🧾 Generar SBOM y Escanear (Syft & Grype)
        if: success()
        run: |
          echo "Generando SBOM con Syft..."
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
          anchore/syft:latest "${{ env.IMAGE_TAG_SHA }}" -o syft-json > sbom.syft.json

          echo "Escaneando SBOM con Grype..."
          docker run --rm \
          -v "$PWD":/work \
          anchore/grype:latest sbom:./sbom.syft.json \
          --fail-on high \
          --only-fixed=false

      # Artefacte SBOM
      - name: 📤 Upload SBOM artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: sbom-syft-json
          path: sbom.syft.json
          if-no-files-found: error
          retention-days: 14

      # 3. 🔑 Login Seguro
      - name: 🔑 Login to Quay.io (Acción Segura)
        if: success()
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USER }}
          password: ${{ secrets.QUAY_PASSWORD }}

      # 4. 🚀 Push Condicional
      - name: 🚀 Push image to Quay.io
        if: success()
        run: |
          docker push ${{ env.IMAGE_TAG_SHA }}
          docker push ${{ env.LATEST_IMAGE_TAG }}
